#include <stdio.h>
int main()
{
    double Height;
    double Width;

    scanf("%lf", &Height);
    scanf("%lf", &Width);
    printf("The Area of the rectangle is %lf\n", Height*Width);
    return 0;

}